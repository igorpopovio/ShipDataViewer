<UserControl
	x:Class="ShipDataViewer.Shell.Gadget"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Width="260"
	Height="100">
	<UserControl.Resources>
		<SolidColorBrush x:Key="GadgetBackground" Color="#FFFFFFFF" />
		<SolidColorBrush x:Key="GadgetBorder" Color="#FFE6E9EE" />
		<Style x:Key="LabelText" TargetType="TextBlock">
			<Setter Property="FontSize" Value="12" />
			<Setter Property="Foreground" Value="#FF6B7280" />
			<Setter Property="TextTrimming" Value="CharacterEllipsis" />
		</Style>
		<Style x:Key="ValueText" TargetType="TextBlock">
			<Setter Property="FontSize" Value="28" />
			<Setter Property="FontWeight" Value="Bold" />
			<Setter Property="Foreground" Value="#FF111827" />
			<Setter Property="TextTrimming" Value="CharacterEllipsis" />
		</Style>
	</UserControl.Resources>

	<Grid>
		<Border
			Padding="10"
			Background="{StaticResource GadgetBackground}"
			BorderBrush="{StaticResource GadgetBorder}"
			BorderThickness="1"
			CornerRadius="10"
			SnapsToDevicePixels="True">
			<Border.Effect>
				<DropShadowEffect
					BlurRadius="8"
					Opacity="0.6"
					ShadowDepth="2"
					Color="#22000000" />
			</Border.Effect>

			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="8" />
					<ColumnDefinition Width="12" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>

				<Rectangle
					Grid.Column="0"
					VerticalAlignment="Stretch"
					Fill="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
					RadiusX="4"
					RadiusY="4" />

				<StackPanel
					Grid.Column="2"
					VerticalAlignment="Center"
					Orientation="Vertical">
					<TextBlock
						Margin="0,0,0,2"
						Style="{StaticResource LabelText}"
						Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}" />
					<TextBlock Style="{StaticResource ValueText}" Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}" />
				</StackPanel>
			</Grid>
		</Border>
	</Grid>
</UserControl>
